# from scoring import didteamsplay

def h2hThreeTeams(key, scores, RawPointCheck, dbData):
    print("3 teams are: " + str(key))
    scores = list(scores)
    for teams in scores:

        print("3 way tie with " + str(teams))
        #Case 4:
        # if didteamsplay(scores,dbData)
    return 0
#No team plays each other
